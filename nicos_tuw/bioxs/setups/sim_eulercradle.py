description = 'motors from the huber euler cratle'

group = 'optional'
excludes = ['eulercradle']

devices = dict(
    # x-axis
    tex_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample translation x-Axis motor',
        abslimits = (-50, 50),
        speed = 10,
        unit = 'mm',
        visibility = (),
    ),
    tex = device('nicos.devices.generic.Axis',
        description = 'Sample translation x-Axis motor',
        motor = 'tex_m',
        precision = 0.001,
    ),
    # y-axis
    tey_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample translation y-Axis motor',
        abslimits = (-50, 50),
        speed = 10,
        unit = 'mm',
        visibility = (),
    ),
    tey = device('nicos.devices.generic.Axis',
        description = 'Sample translation y-Axis motor',
        motor = 'tey_m',
        precision = 0.001,
    ),
    # z-axis
    tez_m = device('nicos.devices.generic.VirtualMotor',
        abslimits = (0, 20),
        speed = 10,
        unit = 'mm',
        visibility = (),
    ),
    tez = device('nicos.devices.generic.Axis',
        description = 'Sample translation z-Axis motor',
        motor = 'tez_m',
        precision = 0.001,
    ),
    # Chi-axis
    phi_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample Rotation phi-Axis motor',
        abslimits = (0, 360),
        speed = 10,
        unit = 'deg',
        visibility = (),
    ),
    phi = device('nicos.devices.generic.Axis',
        description = 'Sample Rotation phi-Axis motor',
        motor = 'phi_m',
        precision = 0.001,
    ),
    # Phi-axis
    chi_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample Rotation Pchihi-Axis motor',
        abslimits = (0, 90),
        speed = 10,
        unit = 'deg',
        visibility = (),
    ),
    chi = device('nicos.devices.generic.Axis',
        description = 'Sample Rotation chi-Axis motor',
        motor = 'chi_m',
        precision = 0.001,
    ),
)
