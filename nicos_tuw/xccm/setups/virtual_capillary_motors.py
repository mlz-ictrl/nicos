description = 'motors moving the capillaries in the optic chamber.'

group = 'lowlevel'

excludes = ['capillary_motors']

devices = dict(
    cap_Ty_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Capillary translation y-Axis motor',
        fmtstr = '%.2f',
        abslimits = (0, 10),
        lowlevel = True,
#       ramp = 1000,
        speed = 2,
        requires = dict(level='guest'),
        unit = 'mm',
    ),
    cap_Ty = device('nicos.devices.generic.Axis',
        description = 'Capillary translation y-axis',
        motor = 'cap_Ty_m',
        precision = 1.0,
    ),
    cap_Tz_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Capillary translation z-Axis motor',
        fmtstr = '%.2f',
        abslimits = (0, 10),
        lowlevel = True,
#       ramp = 1000,
        speed = 2,
        requires = dict(level='guest'),
        unit = 'mm',
    ),
    cap_Tz = device('nicos.devices.generic.Axis',
        description = 'Capillary translation z-axis',
        motor = 'cap_Tz_m',
        precision = 1.0,
    ),
    cap_Rz_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Capillary rotation z-Axis motor',
        fmtstr = '%.2f',
        abslimits = (-10, 10),
        lowlevel = True,
#       ramp = 1000,
        speed = 2,
        requires = dict(level='guest'),
        unit = 'degrees',
    ),
    cap_Rz = device('nicos.devices.generic.Axis',
        description = 'Capillary rotation z-axis',
        motor = 'cap_Rz_m',
        precision = 0.01,
    ),
    cap_Ry_m = device('nicos.devices.generic.VirtualMotor',
        description = 'Capillary rotation y-Axis motor',
        fmtstr = '%.2f',
        abslimits = (-10, 10),
        lowlevel = True,
#       ramp = 1000,
        speed = 2,
        requires = dict(level='guest'),
        unit = 'degrees',
    ),
    cap_Ry = device('nicos.devices.generic.Axis',
        description = 'Capillary rotation y-axis',
        motor = 'cap_Ry_m',
        precision = 0.01,
    ),
)
