# -*- coding: utf-8 -*-

description = 'Axes setup'
group = 'lowlevel'

devices = dict(
    omega_samplestepper = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample stepper omega variant',
        abslimits = (-390, 390),
        speed = 1,
        unit = 'deg',
        precision = 0.001,
    ),
    omega_sampletable = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample table omega variant',
        abslimits = (-10, 390),
        speed = 200,
        unit = 'deg',
        precision = 0.001,
    ),
    x_sampletable = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample table x axis',
        abslimits = (-6, 6),
        speed = 1,
        unit = 'mm',
        precision = 0.005,
    ),
    y_sampletable = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample table y axis',
        abslimits = (-6, 6),
        speed = 1,
        unit = 'mm',
        precision = 0.005,
    ),
    z_sampletable = device('nicos.devices.generic.VirtualMotor',
        description = 'Sample table x axis',
        abslimits = (-1, 90),
        speed = 5,
        unit = 'mm',
        precision = 0.005,
    ),
    theta_monochromator = device('nicos.devices.generic.VirtualMotor',
        description = 'Monochromator theta variant',
        abslimits = (21, 66),
        speed = 2,
        curvalue = 45,
        unit = 'deg',
        precision = 0.001,
    ),
    tilt_monochromator = device('nicos.devices.generic.VirtualMotor',
        description = 'Monochromator tilt',
        abslimits = (-5, 5),
        speed = 1,
        unit = 'deg',
        precision = 0.005,
    ),
    x_monochromator = device('nicos.devices.generic.VirtualMotor',
        description = 'Monochromator x axis',
        abslimits = (-9, 9),
        speed = 1,
        unit = 'mm',
        precision = 0.002,
    ),
    y_monochromator = device('nicos.devices.generic.VirtualMotor',
        description = 'Monochromator y axis',
        abslimits = (-9, 9),
        speed = 1,
        unit = 'mm',
        precision = 0.002,
    ),
    z_monochromator = device('nicos.devices.generic.VirtualMotor',
        description = 'Monochromator z axis',
        abslimits = (0, 30),
        speed = 2,
        unit = 'mm',
        precision = 0.002,
    ),
    theta2_selectorarm = device('nicos.devices.generic.VirtualMotor',
        description = 'Selector arm 2theta variant',
        abslimits = (42, 112),
        speed = 2,
        curvalue = 45,
        unit = 'deg',
        precision = 0.005,
    ),
    d_diaphragm1 = device('nicos.devices.generic.VirtualMotor',
        description = 'Slit 1',
        abslimits = (0, 20),
        speed = 2,
        curvalue = 5,
        unit = 'mm',
        precision = 0.05,
    ),
    d_diaphragm2 = device('nicos.devices.generic.VirtualMotor',
        description = 'Slit 2',
        abslimits = (0, 20),
        speed = 2,
        curvalue = 2,
        unit = 'mm',
        precision = 0.05,
    ),
    theta2_detectorunit = device('nicos.devices.generic.VirtualMotor',
        description = 'Detector unit 2theta variant',
        abslimits = (42, 100),
        speed = 2,
        curvalue = 45,
        unit = 'deg',
        precision = 0.005,
    ),
    z_imageplate = device('nicos.devices.generic.VirtualMotor',
        description = 'Neutron image plate z axis',
        abslimits = (-405, 1),
        speed = 10,
        unit = 'mm',
        precision = 0.01,
    ),
    z_CCD = device('nicos.devices.generic.VirtualMotor',
        description = 'CCD z axis',
        abslimits = (-20, 176),
        speed = 10,
        unit = 'mm',
        precision = 0.01,
    ),
    z_CCDcamera = device('nicos.devices.generic.VirtualMotor',
        description = 'CCD camera z axis',
        abslimits = (-200, 200),
        speed = 10,
        unit = 'mm',
        precision = 0.01,
    ),
    # theta2_CCDcamera = device('nicos.devices.generic.VirtualMotor',
    #     description = 'CCD camera 2theta variant',
    #     abslimits = (-0.5, 107),
    #     speed = 2,
    #     unit = 'deg',
    #     precision = 0.01,
    # ),
    rot_scintillatorhead = device('nicos.devices.generic.VirtualMotor',
        description = 'Scintillator head rotation',
        abslimits = (-10, 95),
        speed = 5,
        unit = 'deg',
        precision = 0.5,
    ),
    # omega_samplegoniometer = device('nicos.devices.generic.VirtualMotor',
    #     description = 'Sample goniometer omega variant',
    #     abslimits = (0, 0),
    # ),
    # x_samplegoniometer = device('nicos.devices.generic.VirtualMotor',
    #     description = 'Sample goniometer x axis',
    #     abslimits = (0, 0),
    # ),
    # y_samplegoniometer = device('nicos.devices.generic.VirtualMotor',
    #     description = 'Sample goniometer y axis',
    #     abslimits = (0, 0),
    # ),
    # rot_diaphragm3 = device('nicos.devices.generic.VirtualMotor',
    #     description = 'Slit 3',
    #     abslimits = (-30000, 913000),
    #     speed = 1000,
    #     unit = 'steps',
    # ),
    # rot_diaphragm4 = device('nicos.devices.generic.VirtualMotor',
    #     description = 'Slit 4',
    #     abslimits = (-30000, 913000),
    #     speed = 1000,
    #     unit = 'steps',
    # ),
)
