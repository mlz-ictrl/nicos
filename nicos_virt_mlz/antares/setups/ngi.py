description = 'Neutron Grating Interferometer'

group = 'optional'

devices = dict(
    G0rz = device('nicos.devices.generic.VirtualMotor',
        description = 'Rotation of G0 grating around beam direction',
        speed = 2.5,
        unit = 'deg',
        abslimits = (-20, 20),
        maxage = 5,
        pollinterval = 3,
        precision = 0.001,
    ),
    G1tx = device('nicos.devices.generic.VirtualMotor',
        description = 'Stepping of G1 perpendicular to the beam direction',
        speed = 100,
        unit = 'um',
        abslimits = (-12500, 12500),
        userlimits = (-12500, 12500),
        maxage = 5,
        pollinterval = 3,
        precision = 0.1,
    ),
    G1tz = device('nicos.devices.generic.VirtualMotor',
        description = 'Translation of G1 parallel to the beam direction',
        speed = 5,
        unit = 'mm',
        abslimits = (-80, 80),
        userlimits = (-80, 80),
        maxage = 5,
        pollinterval = 3,
        precision = 0.001,
    ),
    G1rz = device('nicos.devices.generic.VirtualMotor',
        description = 'Rotation of G1 around the beam axis',
        speed = 5,
        unit = 'deg',
        abslimits = (-20, 20),
        userlimits = (-20, 20),
        maxage = 5,
        pollinterval = 3,
        precision = 0.001,
    ),
    # sry = device('nicos.devices.generic.VirtualMotor',
    #     description = 'Sample rotation',
    #     speed = 5,
    #     unit = 'deg',
    #     abslimits = (-999, 999),
    #     userlimits = (-999, 999),
    #     maxage = 5,
    #     pollinterval = 3,
    #     precision = 0.001,
    # ),
    # G2rz_p = device('nicos.devices.entangle.Motor',
    #     speed = 0.2,
    #     unit = 'deg',
    #     description = 'Rotation of G1 grating around beam direction',
    #     tangodevice = tango_base + 'fzjs7/G1rz',
    #     abslimits = (-400, 400),
    #     maxage = 5,
    #     pollinterval = 3,
    #     precision = 0.0005,
    # ),
    # G2tz = device('nicos.devices.entangle.Motor',
    #     speed = 1,
    #     unit = 'mm',
    #     description = 'Translation of G1 in beam direction. (Talbot distance)',
    #     tangodevice = tango_base + 'fzjs7/G1tz',
    #     abslimits = (0, 20),
    #     maxage = 5,
    #     pollinterval = 3,
    #     precision = 0.05,
    # ),
    # G2rz = device('nicos.devices.entangle.Motor',
    #     speed = 1,
    #     unit = 'deg',
    #     description = 'Rotation of G2 and G1 around beam axis',
    #     tangodevice = tango_base + 'fzjs7/G12rz',
    #     abslimits = (-400, 400),
    #     userlimits = (-250, 250),
    #     maxage = 5,
    #     pollinterval = 3,
    #     precision = 0.01,
    # ),
)
