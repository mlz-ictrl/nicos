description = 'Primary slit devices'

group = 'lowlevel'

devices = dict(
    pst_m = device('nicos.devices.generic.VirtualMotor',
        abslimits = (-15, 15),
        unit = 'mm',
        curvalue = 0,
        speed = 0.2,
        fmtstr = '%.2f',
        lowlevel = True,
    ),
    pst_c = device('nicos.devices.generic.VirtualCoder',
        motor = 'pst_m',
        fmtstr = '%.2f',
        lowlevel = True,
    ),
    pst = device('nicos.devices.generic.Axis',
        description = 'Primary slit translation (PST)',
        motor = 'pst_m',
        coder = 'pst_c',
        precision = 0.01,
    ),
    psz_m = device('nicos.devices.generic.VirtualMotor',
        abslimits = (-15, 15),
        unit = 'mm',
        speed = 0.2,
        fmtstr = '%.2f',
        lowlevel = True,
    ),
    psz_c = device('nicos.devices.generic.VirtualCoder',
        motor = 'psz_m',
        fmtstr = '%.2f',
        lowlevel = True,
    ),
    psz = device('nicos.devices.generic.Axis',
        description = 'Primary slit Z translation (PSZ)',
        motor = 'psz_m',
        coder = 'psz_c',
        precision = 0.01,
    ),
    psx_m = device('nicos.devices.generic.VirtualMotor',
        abslimits = (-120, 120),
        unit = 'mm',
        speed = 2,
        fmtstr = '%.2f',
        lowlevel = True,
    ),
    psx = device('nicos.devices.generic.Axis',
        description = 'Primary optics translation in beam direction',
        fmtstr = '%.2f',
        motor = 'psx_m',
        precision = 0.01,
    ),
)
