# pylint: skip-file

# test: subdirs = estia
# test: setups = cart,driver,interferometer,motor_temp,mover,robot,startup,temp,vacuum
# test: needs = p4p, epics
# test: needs = streaming_data_types
# test: needs = confluent_kafka
# test: needs = yuos_query

read()
status()
