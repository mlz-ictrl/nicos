description = 'collimators and lengths'

devices = dict(
    L0  = device('nicos.generic.ManualMove',
                 description = 'distance core - virtual source',
                 default = 3410,
                 unit = 'mm',
                 fmtstr = '%.0f',
                 abslimits = (3410, 3410)),
    L1  = device('nicos.generic.ManualMove',
                 description = 'distance virtual source - monochromator',
                 default = 2090,
                 unit = 'mm',
                 fmtstr = '%.0f',
                 abslimits = (2090, 2090)),
    L2  = device('nicos.generic.ManualMove',
                 description = 'distance monochromator - sample',
                 default = 2115,  # or 2150 ?!
                 unit = 'mm',
                 fmtstr = '%.0f',
                 abslimits = (2115, 2115)),
    L3  = device('nicos.generic.ManualMove',
                 description = 'distance sample - analyser',
                 default = 880,
                 unit = 'mm',
                 fmtstr = '%.0f',
                 abslimits = (880, 890)),
    L4  = device('nicos.generic.ManualMove',
                 description = 'distance analyser - detector',
                 default = 900,
                 unit = 'mm',
                 fmtstr = '%.0f',
                 abslimits = (900, 900)),
)
