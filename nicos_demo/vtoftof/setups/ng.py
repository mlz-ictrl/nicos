description = 'elliptical neutron guide nose'

group = 'optional'

devices = dict(
    ng_left = device('nicos.devices.generic.VirtualMotor',
        description = 'Left mirror bender of the flexible neutron guide',
        fmtstr = '%.0f',
        abslimits = (-20000.0, 20000.),
        unit = 'steps',
        speed = 500,
        curvalue = 1000,
        requires = {'level': 'admin'},
    ),
    ng_left_sw = device('nicos.devices.generic.ManualSwitch',
        description = 'Limit switch of the left motor',
        states = (0, 1),
        fmtstr = '%d',
        lowlevel = True,
    ),
    ng_right = device('nicos.devices.generic.VirtualMotor',
        description = 'Right mirror bender of the flexible neutron guide',
        fmtstr = '%.0f',
        abslimits = (-20000.0, 20000.),
        unit = 'steps',
        speed = 500,
        curvalue = 1000,
        requires = {'level': 'admin'},
    ),
    ng_right_sw = device('nicos.devices.generic.ManualSwitch',
        description = 'Limit switch of the right motor',
        states = (0, 1),
        fmtstr = '%d',
        lowlevel = True,
    ),
    ng_bottom = device('nicos.devices.generic.VirtualMotor',
        description = 'Bottom mirror bender of the flexible neutron guide',
        fmtstr = '%.0f',
        abslimits = (-20000.0, 20000.),
        unit = 'steps',
        speed = 500,
        curvalue = 1000,
        requires = {'level': 'admin'},
    ),
    ng_bottom_sw = device('nicos.devices.generic.ManualSwitch',
        description = 'Limit switch of the bottom motor',
        states = (0, 1),
        fmtstr = '%d',
        lowlevel = True,
    ),
    ng_top = device('nicos.devices.generic.VirtualMotor',
        description = 'Top mirror bender of the flexible neutron guide',
        fmtstr = '%.0f',
        unit = 'steps',
        abslimits = (-20000.0, 20000.),
        curvalue = 1000,
        requires = {'level': 'admin'},
    ),
    ng_top_sw = device('nicos.devices.generic.ManualSwitch',
        description = 'Limit switch of the top motor',
        states = (0, 1),
        fmtstr = '%d',
        lowlevel = True,
    ),
    ng_focus = device('nicos.devices.generic.Slit',
        description = 'Focussing neutron guide',
        left = 'ng_left',
        right = 'ng_right',
        bottom = 'ng_bottom',
        top = 'ng_top',
        opmode = '4blades',
        pollinterval = 5,
        maxage = 10,
        fmtstr = '%.0f %.0f %.0f %.0f',
        requires = {'level': 'admin'},
    ),
)
